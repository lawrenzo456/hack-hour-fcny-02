//You're given an array of integers a and an integer k. Your task is to calculate the number of ways to pick two different indices i<j such that a[i] + a[j] is divisible by k

//Given an array of integers a. The task is to return another array b where b[i] = a[i-1] + a[i] + a[i+1], (if the element does not exist, it should be 0

//Given a list of words (consisting of lowercase English letters) and a complex word written in camelCase (consisting of English letters), check if the complex word consists of words from the given list

//Given two strings, merge them with the following merge function: instead of comparing the characters in the usual lexicographical order, compare them based on how many times they occur in their respective strings. Fewer occurrences mean the character is considered smaller; in case of equality, compare them lexicographically; in case of equality, take teh character from the first string.

//Create. adata structure that allows the following operations:
//insert xy - insert an object with key x and value y
//get x - return the value of an object with key x
//addToKey x - add x to all keys in map
//addToValye y - add y to all values in map
//Time complexity of each operation should be O(log(N))